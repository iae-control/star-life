# 별의 일생 — 프로덕션 개발 계획서 (Claude Code용)

이 문서를 새 저장소 루트에 `PLAN.md`로 두고, Claude Code에게 "PLAN.md를 읽고 M0부터 순서대로 진행"이라고 지시하는 이관 문서다. 각 마일스톤은 완료 기준을 만족하면 멈추고 사용자 승인 후 다음으로 넘어간다. 현재의 데모(v4 단일 HTML `별의일생.html`)는 `legacy/` 폴더에 넣어 게임 로직·밸런스 레퍼런스로만 쓴다.

## 0. 목표와 정체성

목표는 데모를 프로덕션 수준의 완성작으로 끌어올리는 것이다. 프로덕션 수준의 정의: 보급형 안드로이드 폰에서 60fps 고정, 30분 세션 무크래시, 캠페인 콘텐츠(레벨 6개·적 15종 내외·보스 6종), 저장·설정·튜토리얼·한/영 로컬라이즈, 스토어 제출이 가능한 품질과 라이선스 상태.

게임의 정체성은 제목 "별의 일생"에서 출발한다. [제안 — 사용자 승인 필요] 캠페인을 별의 진화 단계로 구성한다: L1 성운 → L2 원시별 → L3 주계열성 → L4 적색거성 → L5 초신성 → L6 블랙홀. 최종 레벨의 블랙홀 보스는 슈퍼무기 "Jungjioo"의 블랙홀 연출과 세계관적으로 호응한다. 확정 사양으로 유지할 시그니처: 슈퍼무기 발동 시 "hey! I am Jungjioo!!" 말풍선과 무적, 하단 블랙홀 3~4개, "Spid!!"와 함께 청백색 환영 함선 러시(대사 표기는 이대로가 정본).

## 1. 기술 스택 (기본값)

- 언어/빌드: TypeScript(strict) + Vite. 코드 품질은 ESLint + Prettier.
- 엔진: Phaser 3 (WebGL 렌더, 텍스처 아틀라스, 파티클, 오디오, 씬 관리). 슈팅게임에 필요한 기반이 검증돼 있고 모바일 WebGL 성능이 좋아 개발 속도와 품질을 동시에 확보한다.
- 패키징: Capacitor — Android(디버그 APK 배포 + 릴리스 AAB), iOS 프로젝트 스캐폴드(빌드는 맥 확보 시). 웹 빌드는 itch.io 또는 GitHub Pages 배포.
- 테스트: vitest — 렌더와 무관한 순수 로직(데미지 계산, 경제, 웨이브 파서, 저장 마이그레이션)만 대상.
- CI(선택): GitHub Actions — 웹 빌드 + 디버그 APK 아티팩트 자동 생성.
- 대안 경로: 의존성 없는 커스텀 캔버스 엔진(현 데모 확장). 초경량·완전 통제가 필요할 때만 검토하고, 기본은 Phaser로 간다. M1 종료 시점에 성능·생산성을 보고 1회 재평가한다.

## 2. 에셋 전략 (신규 제작 허용 반영)

- 그래픽 1순위: Kenney CC0 팩(Space Shooter Redux 등). 표기 의무가 없어 스토어 배포·상업화에 가장 안전하다. 부족한 스프라이트(플레이어 기체, 보스, 테마 배경)는 팔레트를 맞춰 신규 제작한다 — Aseprite 수작업 또는 스크립트 생성(파티클·배경은 절차 생성이 오히려 품질이 좋다).
- 기존 Tyrian CC-BY 에셋은 사용을 중단한다. 표기 의무 관리가 사라져 배포가 단순해진다(데모 크레딧 문구도 신작에서는 제거).
- 아트 규격: 논리 해상도 360x640(9:16 세로), 스프라이트 32/48/64px 급 계층, 공통 팔레트와 1px 아웃라인 규칙을 `assets/STYLE.md`로 문서화.
- 사운드: SFX는 ZzFX(절차 생성)와 Kenney CC0 오디오. 음악은 CC0 칩튠 3~4곡부터 시작하고(레벨 분위기별), 마음에 안 차면 커미션을 검토한다.
- 폰트: 한글 지원 픽셀 폰트(예: Galmuri — 라이선스 확인 후 채택). 타이틀 "별의 일생"의 한글 렌더링 품질을 확보한다.
- 모든 외부 에셋은 `assets/LICENSES.md`에 출처·라이선스를 기록한다. 이 대장이 없으면 스토어 제출 불가로 간주한다.

## 3. 아키텍처

- 씬 구성: Boot → Preload → Title → StageIntro → Game → Shop → Result, 오버레이로 Pause/Settings, 별도로 Credits.
- 데이터 주도 설계가 원칙이다. `/src/data/`의 JSON이 콘텐츠의 단일 출처: `weapons.json`, `enemies.json`(행동 파라미터), `bosses.json`(페이즈), `waves/*.json`(레벨별 타임라인), `levels.json`(배경·BGM·스크롤), `shop.json`(가격 곡선), `i18n/ko.json·en.json`. 스키마는 zod로 검증하고 로드 실패 시 명확한 에러를 낸다.
- 시스템 모듈: InputRouter(터치 드래그/키보드/게임패드 통합), ObjectPool(탄·적·이펙트 전부 풀링), Combat(AABB→필요 시 원형 판정, 데미지 파이프라인), WaveDirector(타임라인 실행), SaveSystem(버전드 스키마 + 마이그레이션, 이중 기록으로 손상 방지), AudioBus(SFX/BGM 볼륨 분리, 덕킹), Fx(셰이크·히트스톱·플래시·슬로모), Haptics(Capacitor, 설정으로 on/off).
- 저장 매체: 웹은 localStorage, 앱은 Capacitor Preferences(데모의 "저장 없음"은 아티팩트 환경 제약이었을 뿐이므로 정식 도입).
- 디버그 도구: `?debug` 진입 시 무적 토글, 레벨/웨이브 점프, 히트박스 표시, FPS·드로우콜 HUD, 웨이브 타임라인 스크럽. 콘텐츠 제작 속도를 좌우하므로 M2에서 우선 구축한다.

## 4. 게임 디자인 사양

- 조작: 터치 드래그(손가락 오프셋·감도 설정 제공, 오토파이어), 키보드(방향키/WASD+발사/슈퍼), 게임패드. 좌우반전·버튼 위치 설정 포함.
- 플레이어 성장: 실드/아머 이중 체력(데모 계승), 전방무기 6종(현 5종 리밸런스 + 신규 1종), 후방무기 3종, 사이드킥(옵션 위성) 2종, 슈퍼 "Jungjioo"(충전제). 무기별 파워 1~6은 유지.
- 적 설계: 공용 행동 시스템을 파라미터화한다 — 진입 경로(직선/사인/베지어), 사격 패턴(조준/부채꼴/링/유도), 체력·드랍. 레벨당 신규 2~3종으로 총 15종 내외.
- 보스 6종: 각 레벨 테마와 결합(예: L1 성운 아메바형 분열체, L3 코로나 링을 두른 항성체, L6 블랙홀 — 탄을 빨아들이는 기믹). 보스마다 2~3페이즈와 약점 연출.
- 이코노미: 크레딧 수입(레벨별 기대값)과 상점 가격 곡선을 `balance.md` + JSON으로 관리. "격파 크레딧 = 점수"였던 데모 방식은 분리(점수와 화폐를 따로).
- 난이도: Easy/Normal/Hard 선택 + 플레이 성과 기반 미세 랭크. 캠페인 클리어 후 엔들리스 모드 해금.
- 튜토리얼: 레벨1 도입 90초 안에 이동·발사·오브·슈퍼 사용을 자연스럽게 학습시키는 스크립트 웨이브.

## 5. 마일스톤

### M0 — 프로젝트 셋업

1. Vite+TS+Phaser 스캐폴드, ESLint/Prettier, 씬 뼈대(Boot~Game 빈 화면), 논리 해상도·스케일링 확정.
2. Capacitor 초기화(android 추가), 디버그 APK 빌드 스크립트 정리.
3. 완료 기준: 웹과 안드로이드 디버그 빌드 양쪽에서 빈 게임 씬이 60fps로 구동.

### M1 — 데모 패리티 이식

1. `legacy/별의일생.html`의 전 기능(이동·무기 5종·적 3종·보스·상점·오브·슈퍼 Jungjioo 연출·HUD)을 Phaser로 재구현. 임시로 데모의 절차 이펙트 방식 유지 가능.
2. 전면 풀링, 텍스처 아틀라스 파이프라인 구축(임시 에셋으로).
3. 완료 기준: 데모와 동등 이상 플레이 품질, 30분 무크래시, 폰 실기 60fps.

### M2 — 데이터 주도화 & 제작 도구

1. 4장의 JSON 스키마 확정 + zod 검증, 기존 콘텐츠를 데이터로 이전.
2. 웨이브/밸런스 핫리로드(개발 서버), `?debug` 도구 일체 구축.
3. 완료 기준: 코드 수정 없이 JSON 편집만으로 새 적·새 웨이브가 게임에 등장.

### M3 — 콘텐츠 1차 (L1~L3)

1. 신규 아트 스타일 확정(`STYLE.md` + 플레이어 기체 리디자인), Kenney 기반 에셋 편성.
2. 레벨 1~3 배경(패럴랙스)·웨이브·신규 적 6~9종·보스 3종.
3. 완료 기준: L1~L3 연속 플레이 15분 분량, 스타일 통일.

### M4 — 콘텐츠 2차 + 성장 시스템 (L4~L6)

1. 레벨 4~6, 신규 적·보스 완성(최종 블랙홀 보스 포함).
2. 후방무기·사이드킥·상점 개편·난이도 3종·저장(진행도/설정/기록).
3. 완료 기준: 엔딩까지 30~40분 캠페인 완주 가능, 저장·이어하기 동작.

### M5 — 폴리시

1. 사운드·음악 전면 적용, 햅틱, 히트스톱·슬로모·보스 연출 강화, 튜토리얼.
2. 설정(볼륨·진동·조작·언어), 한/영 로컬라이즈, 성능 최적화(저사양 폰 60fps, 드로우콜 예산).
3. 완료 기준: 6장의 품질 체크리스트 전 항목 통과.

### M6 — 릴리즈

1. 릴리스 키스토어 생성 절차 안내(백업 필수) → 서명 APK(직배포용)와 AAB(플레이 스토어용) 산출.
2. 앱 아이콘·스플래시·스토어 문구·스크린샷, 웹 빌드 배포(itch.io), 크레딧 화면(에셋 라이선스 대장 반영).
3. 베타 테스트 라운드(지인 배포 APK) → 피드백 반영 1회.
4. 완료 기준: 배포 산출물 3종(웹 URL, APK, AAB) + 라이선스 대장 완비.

## 6. 품질 기준 (Definition of Done)

- 성능: 보급형 폰(갤럭시 A 시리즈급)에서 60fps, 드로우콜 100 미만, 플레이 중 GC 스파이크로 인한 프레임 드랍 없음.
- 안정성: 30분 세션 무크래시, 전화·홈 이동 인터럽트 후 안전 복귀, 저장 손상 방지(이중 기록·버전 마이그레이션).
- UX: 설치 후 60초 안에 첫 플레이 도달, 모든 화면 터치만으로 조작 가능, 일시정지 완비.
- 코드: TS strict 통과, 핵심 로직 vitest 커버, 데이터 전량 스키마 검증, `npm run build`와 APK 빌드가 문서화된 명령 한 줄로 재현.

## 7. 사전 준비 — 사용자가 할 일

1. Node LTS와 Git 설치.
2. Android Studio(SDK 34+)와 JDK 17 설치 — M0에서 바로 필요.
3. 릴리스 단계(M6)에서 키스토어를 만들게 되는데, 이 파일과 비밀번호는 분실 시 앱 업데이트가 영구히 불가능하므로 백업 계획을 미리 정해둘 것.
4. 선택: Aseprite(도트 직접 수정 원할 때), 맥(추후 iOS 빌드).

## 8. 리스크와 결정 대기 항목

- 캠페인 테마(별의 진화 6단계)는 제안 상태 — M3 착수 전 승인 필요. 반려 시 레벨 테마만 교체하고 구조는 유지.
- Phaser 채택은 M1 종료 시 1회 재평가(성능·개발 감각). 이후에는 변경하지 않는다.
- 음악은 CC0 선차용 → 불만족 시 커미션. 크레딧 화면은 어떤 경우든 유지.
- iOS는 맥·개발자 계정($99/년) 확보 전까지 웹 링크로 대응. Capacitor 구조상 확보 즉시 빌드 가능하게 유지한다.
- "별의 일생" 명칭·"Jungjioo" 대사는 고유 시그니처로 확정 — 임의 변경 금지.

## 9. 저장소 구조

```
star-life/
├─ PLAN.md                # 이 문서
├─ legacy/별의일생.html    # v4 데모 (레퍼런스)
├─ src/
│  ├─ scenes/  ├─ systems/  ├─ data/  ├─ ui/
├─ assets/    # 원본 아트·오디오 + STYLE.md + LICENSES.md
├─ public/    # 빌드 포함 리소스
├─ android/   # Capacitor
└─ tests/
```

## 10. Claude Code 킥오프 프롬프트 (복사용)

이 저장소의 PLAN.md를 읽고 M0부터 시작해줘. 각 마일스톤의 완료 기준을 만족하면 멈추고 결과(실행 방법 포함)를 보고한 뒤 내 승인을 받고 다음으로 넘어가. 캠페인 테마(8장)는 M3 전에 나에게 승인을 받아. legacy 데모는 로직 레퍼런스로만 쓰고, 에셋은 2장의 전략(CC0 우선 + 신규 제작)을 따라. "별의 일생" 명칭과 슈퍼무기 "Jungjioo" 연출 사양은 변경하지 마.
